import ChatContainer from "../containers/ChatContainer";

const Chat = () => {
  // const lastMessage = messages[messages.length - 1];
  return (
    <>
      <ChatContainer />
    </>
  );
};

export default Chat;
